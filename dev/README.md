The `dev/` directory is where you can place your working themes and plugins.

Symlink them into the sandbox WP instance:

    $ sudo ln -sf dev/theme/my_cool_theme /var/www/sandbox_wp/wp-content/themes/
    
